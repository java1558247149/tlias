<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.ClazzMapper">
    <update id="setClazz">
        update clazz
        <set>
            <if test="name != null and name !=''">
                name=#{name},
            </if>
            <if test="room != null and room !=''">
                room=#{room},
            </if>
            <if test="beginDate != null">
                begin_date=#{beginDate},
            </if>
            <if test="endDate != null">
                end_date=#{endDate},
            </if>
            <if test="masterId != null">
                master_id=#{masterId},
            </if>
            <if test="subject != null">
                subject=#{subject},
            </if>
            <if test="updateTime != null">
                update_time=#{updateTime}
            </if>
        </set>
        where id=#{id}
    </update>

    <select id="getWhereCheck" resultType="com.itheima.pojo.Clazz">
        SELECT c.*, e.name masterName
        FROM clazz c
        LEFT JOIN emp e ON c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name = #{name}
            </if>
            <if test="begin != null and end != null">
                AND c.begin_date >= #{begin}
            </if>
            <if test="end != null">
                AND c.end_date &lt;= #{end}
            </if>
        </where>
    </select>
    <select id="getClazzCount" resultType="java.util.Map">
        select (case clazz_id when 1 then 'JavaEE就业163期'
        when 2 then 'JavaEE就业1期'
        when 4 then '前端就业90期'
        when 5 then 'JavaEE就业165期'
        when 6 then 'JavaEE就业166期'
        when 7 then '大数据就业58期'
        when 8 then'JavaEE就业167期'
        else '其他' end) pos, count(*) num
        from student group by clazz_id;
    </select>
</mapper>